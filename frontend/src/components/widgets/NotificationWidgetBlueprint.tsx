import React from "react";
import { Box, makeStyles } from "@material-ui/core";


const useStyles = makeStyles(
  (theme) => (
    {}
  )
);


interface NotificationWidgetBlueprintProps {

}


const NotificationWidgetBlueprint: React.FC<NotificationWidgetBlueprintProps> = ({}) => {

  const classes = useStyles();

  return (
    <Box>

    </Box>
  );

};


export default NotificationWidgetBlueprint;
